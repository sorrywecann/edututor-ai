# Trained model weights (auto-downloaded, do not commit)
tutor-service/models/sentiment/output/
tutor-service/models/audio2lipsync/*.pt
tutor-service/models/piper/*.onnx

# Python
tutor-service/venv/
tutor-service/.venv/
tutor-service/data/
tutor-service/__pycache__/
tutor-service/**/__pycache__/
tutor-service/**/*.pyc
tutor-service/.pytest_cache/
tutor-service/app/**/__pycache__/

# Environment & secrets
.env
tutor-service/.env
core/.env.local
credentials.json
# v0.8.0 security audit additions (S4)
.env.local
.env.*.local
*.key
*.pem
secrets/
.aws/

# Node / Next.js
core/node_modules/
core/.next/
core/out/
core/tsconfig.tsbuildinfo

# Runtime data (generated locally)
data/
.playwright-mcp/
.stack-pids.json
core/test-results/
core/playwright-report/

# Audio references (development only)
tutor-service/models/chatterbox/references/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Visual companion
.superpowers/

# Local tooling / working notes (not for public repo)
.opencode/
.dev/
.claude/
.sisyphus/

# UE5 packaged shipping builds (3+ GB, binary, regenerated by Roland)
# Brief: docs/UE5-WINDOWS-TEST-BRIEF.md
Edutor*/
EdutorUE*/
EdotutorUE*/

# Local logs (load test runners, backend dev logs)
logs/
*.log
*.err
!docs/load_tests_v5/**/*.log

# k6 raw event streams (large, regeneratable from scripts/load_test_runner.sh)
# Keep summary, system_status, prom_metrics, memory_timeline — only ignore raw output
docs/load_tests_v5/**/k6_output.json

# Marketing/design iteration screenshots (root-level only — not docs/)
# Drop the deliverable copies into docs/output3/visuals/ if needed.
/*.png
!consolidated-landing.png
!consolidated-onepager.png

# Backup of historical tech doc HTML (canonical lives in docs/)
/EduTutor_AI_Technicka_Dokumentacia_v*.html
/EduTutor_AI_Technicka_Dokumentacia_v*\ *.html

# macOS Finder Cmd-D + iCloud "Conflict" duplicates (e.g. "foo 2.md")
**/* 2.*
**/* copy.*
**/* (Conflict).*
