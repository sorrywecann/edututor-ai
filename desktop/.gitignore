# Electron runtime + deps (huge; restored via `npm install`)
node_modules/

# electron-builder output
dist/
out/

# staged bundle (regenerated by the build: python runtime, frontend, wilbur, ue5)
resources/

# local machine path/port overrides (dev-specific)
launcher.config.json

*.log
