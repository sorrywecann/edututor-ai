# v0.6.2 Phase 1 Verification — Build Integrity Report

**Date:** 2026-05-30 04:08 CEDT
**Verified by:** Claude Opus 4.7 (session continuation)
**Build:** `desktop/dist/EduTutor-Setup-0.6.2.exe`

## Summary

**5/5 checks PASS. Build is ready for Phase 2 (test machine install).**

| Check | Status | Evidence |
|---|---|---|
| 1.1 EXE artifact | ✅ PASS | 928 401 429 B, built 03:32 30-May, SHA256 `F6C072F8...0F27B`, blockmap present |
| 1.2 Frontend bundle voice diagnostics | ✅ PASS | 8 v0.6.2 voice console strings present in production chunks |
| 1.3 Backend dual-stack + DeepSeek + 22 routes | ✅ PASS | run_dev.py:90-94 IPV6_V6ONLY=0, main.py:22 eager torch.nn, system.py:779-1028 DeepSeek |
| 1.4 app.asar contents | ✅ PASS | version `0.6.2`, ensureHfCacheExtracted, HF_HOME writable, mid-session monitor, httpOk 5s |
| 1.5 Bundled Python + deps + HF cache | ✅ PASS | 4 VC++ DLLs, python311._pth, 11 critical deps (torch/transformers/etc.), HF cache 458 MB |

## SHA-256 record

```
F6C072F8ABDDBAB680654B3C1D798B99D6A4C983DEC003BC325EF474ADD0F27B  EduTutor-Setup-0.6.2.exe
```

## Detail: voice diagnostic strings in production bundle

Found in `dist/win-unpacked/resources/frontend.tgz → .next/static/chunks/app/(shell)/page-04733f509b83916a.js`:

```
"[voice-watchdog] RESCUE  "
"[voice] audio safety timeout fired after "
"[voice] queue empty + stream done — restarting listener  (active="
"[voice] queue empty, but stream still open — waiting for next sentence"
"[voice] session END"
"[voice] session START"
"[voice] startListening called with stale audio state (playing="
"[voice] stream END  (queueLen="
"[voice] stream END: queue already drained — restart listener directly"
```

These will appear in DevTools Console during test scenario 12 — providing deterministic root-cause data for voice loop bug.

## Detail: orchestrator.mjs critical lines (extracted from app.asar)

```
35:  ue5Audio:    false,           // EDU_UE5_AUDIO — KEEP OFF until Martin's RAI Blueprint ships
105: function httpOk(url, timeoutMs = 5000) {                       ← v0.6.1 fix
276:          if (CONFIG.ue5Audio) env.EDU_UE5_AUDIO = '1';         ← locked architectural decision
303:            // v0.6.2: HF_HOME points at the WRITABLE per-user dir, same path
309:            env.HF_HOME = join(this.dataDir, 'hf-cache');       ← v0.6.2 writable HF
310:            try { mkdirSync(env.HF_HOME, { recursive: true }); }
491:    this._startUE5MidSessionMonitor();                          ← v0.6.1
494:  _startUE5MidSessionMonitor() {                                ← UE5 reconnect
```

**Notably absent (correctly):** `HF_HUB_OFFLINE`, `TRANSFORMERS_OFFLINE` — those caused the v0.6.0 STT regression and are gone in v0.6.2.

## Bundled Python deps verified

| Dep | Size | Purpose |
|---|---|---|
| torch | 414 MB | Tensor library |
| scipy | 72 MB | Numerical computing |
| av.libs | 65 MB | FFmpeg for PyAV (soundfile) |
| chromadb_rust_bindings | 61 MB | Vector DB |
| ctranslate2 | 60 MB | faster-whisper C++ runtime |
| transformers | 49 MB | HF transformers |
| onnxruntime | 34 MB | ChromaDB default |
| sklearn | 22 MB | ML metrics |
| numpy + numpy.libs | 36 MB | Vector math |
| sentence_transformers | (small) | RAG embedding |
| faster_whisper | (small) | STT |
| edge_tts | (small) | Slovak TTS |
| openai SDK | (small) | OpenAI/DeepSeek cloud LLM |
| anthropic SDK | (small) | Claude cloud LLM |

Plus 4 VC++ runtime DLLs (msvcp140 + msvcp140_1 + vcruntime140 + vcruntime140_1) and `python311._pth` isolation marker — ensures clean Windows boot.

## What this verification does NOT cover

- ❌ Runtime behavior on clean Windows machine — **Phase 2 + 3 task**
- ❌ Lipsync working end-to-end — **Phase 3 scenario 11**
- ❌ Voice loop fix — **Phase 3 scenario 12**
- ❌ Code signing (Defender SmartScreen warning expected on test machine)

## Next steps

1. **User: copy `EduTutor-Setup-0.6.2.exe` to test machine** (or run `docs\install-v0.6.2.ps1` on dev machine for quick local verification)
2. **User: run `docs\install-v0.6.2.ps1`** on test machine (silent uninstall prior + clean install)
3. **User: open second terminal, run `docs\monitor.ps1`** (rolling live state)
4. **User: launch app, follow `docs\test-runs\v0.6.2-self-test-template.md`**
5. **User: capture DevTools Console transcript during scenario 12**
6. **Claude: analyze transcript → identify voice loop root cause → surgical fix → v0.6.3**

---

**Phase 1 closed.** Build integrity ✅. Ready for Phase 2.
